from django.contrib import admin
from octofit_tracker.models import User, Team, Activity, Workout, Leaderboard

# Note: MongoEngine models can't be registered with Django admin directly.
# You can use a custom admin or a third-party package for MongoEngine admin if needed.

# Placeholder for future admin integration
